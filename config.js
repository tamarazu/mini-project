const env = process.env.NEXT_PUBLIC_API_LINK
// const env = 'testing'
// const env = ((process.env.NEXT_PUBLIC_MODE === 'local') || (process.env.NEXT_PUBLIC_MODE === 'staging')) ? dev : prod;

export default env;